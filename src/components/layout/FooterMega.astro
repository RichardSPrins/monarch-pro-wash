---
import Logo from "@/components/layout/Logo.astro";
import { getBusinessData, getFooterNav } from "@/lib/utils";
import { getWidgetArea } from "emdash";
import { WidgetArea } from "emdash/ui";

const business = await getBusinessData();
const year = new Date().getFullYear();

// Footer columns come from the native, CMS-editable `footer` menu: each top-level
// group is a column, its children are the links. Legal from `footer-legal`.
const { groups, legal } = await getFooterNav();

// Native editable slot — rendered only when an editor has added widgets to it.
const footerExtra = await getWidgetArea("footer-extra");
---

<footer class="bg-bg-inverse text-fg-inverse">
  <!-- Newsletter / CTA strip -->
  <div class="border-b border-fg-inverse/10">
    <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-12">
      <div
        class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-8"
      >
        <div class="max-w-xl">
          <h2 class="text-2xl sm:text-3xl font-bold text-fg-inverse mb-2">
            Ready to get started?
          </h2>
          <p class="text-fg-inverse/60 text-sm leading-relaxed">
            {
              business.tagline ||
                `Get in touch with ${business.businessName} today and see how we can help.`
            }
          </p>
        </div>
        <div
          class="flex flex-col sm:flex-row items-stretch sm:items-center gap-3 shrink-0"
        >
          {
            business.phone && (
              <a
                href={`tel:${business.phone}`}
                class="inline-flex items-center justify-center gap-2 rounded-lg bg-accent text-accent-foreground font-semibold px-6 py-3 text-sm hover:opacity-90 transition-opacity"
              >
                <svg
                  class="w-4 h-4 shrink-0"
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  aria-hidden="true"
                >
                  <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 13a19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 3.6 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z" />
                </svg>
                Call {business.phone}
              </a>
            )
          }
          <a
            href="/contact"
            class="inline-flex items-center justify-center rounded-lg border border-fg-inverse/20 text-fg-inverse font-semibold px-6 py-3 text-sm hover:bg-fg-inverse/10 transition-colors"
          >
            Contact Us
          </a>
        </div>
      </div>
    </div>
  </div>

  <!-- Main footer: many columns -->
  <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-16">
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-10">
      <!-- Brand / NAP column -->
      <div class="lg:col-span-1">
        <a
          href="/"
          class="inline-block font-bold text-xl text-fg-inverse hover:text-accent transition-colors mb-4"
        >
          <Logo business={business} />
        </a>
        {
          business.tagline && (
            <p class="text-fg-inverse/60 text-sm leading-relaxed mb-6">
              {business.tagline}
            </p>
          )
        }
        <div class="flex flex-col gap-3 text-sm">
          {
            business.phone && (
              <a
                href={`tel:${business.phone}`}
                class="flex items-center gap-2 text-fg-inverse/70 hover:text-fg-inverse transition-colors"
              >
                <svg
                  class="w-4 h-4 shrink-0"
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  aria-hidden="true"
                >
                  <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 13a19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 3.6 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z" />
                </svg>
                {business.phone}
              </a>
            )
          }
          {
            business.email && (
              <a
                href={`mailto:${business.email}`}
                class="flex items-center gap-2 text-fg-inverse/70 hover:text-fg-inverse transition-colors"
              >
                <svg
                  class="w-4 h-4 shrink-0"
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  aria-hidden="true"
                >
                  <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z" />
                  <polyline points="22,6 12,12 2,6" />
                </svg>
                {business.email}
              </a>
            )
          }
          {
            business.address && (
              <address class="flex items-start gap-2 text-fg-inverse/70 not-italic">
                <svg
                  class="w-4 h-4 shrink-0 mt-0.5"
                  xmlns="http://www.w3.org/2000/svg"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                  aria-hidden="true"
                >
                  <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" />
                  <circle cx="12" cy="10" r="3" />
                </svg>
                <span>
                  {business.address}, {business.city}, {business.state}{" "}
                  {business.zip}
                </span>
              </address>
            )
          }
        </div>
      </div>

      <!-- Nav columns — one per group in the native `footer` menu -->
      {
        groups.map((group) => (
          <div>
            <h3 class="text-sm font-semibold text-fg-inverse uppercase tracking-wider mb-4">
              {group.label}
            </h3>
            <ul class="flex flex-col gap-2.5 list-none m-0 p-0">
              {(group.children ?? []).map((item) => (
                <li>
                  {item.href ? (
                    <a
                      href={item.href}
                      class="text-sm text-fg-inverse/60 hover:text-fg-inverse transition-colors"
                    >
                      {item.label}
                    </a>
                  ) : (
                    <span class="text-sm text-fg-inverse/60">{item.label}</span>
                  )}
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </div>

    <!-- Social row -->
    {
      (business.social?.facebook ||
        business.social?.instagram ||
        business.social?.youtube) && (
        <div class="flex items-center gap-3 mt-12 pt-8 border-t border-fg-inverse/10">
          {business.social?.facebook && (
            <a
              href={business.social.facebook}
              target="_blank"
              rel="noopener"
              aria-label="Facebook"
              class="w-9 h-9 flex items-center justify-center rounded-lg bg-fg-inverse/10 text-fg-inverse/60 hover:bg-fg-inverse/20 hover:text-fg-inverse transition-colors"
            >
              <svg
                class="w-4 h-4"
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
                fill="currentColor"
                aria-hidden="true"
              >
                <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z" />
              </svg>
            </a>
          )}
          {business.social?.instagram && (
            <a
              href={business.social.instagram}
              target="_blank"
              rel="noopener"
              aria-label="Instagram"
              class="w-9 h-9 flex items-center justify-center rounded-lg bg-fg-inverse/10 text-fg-inverse/60 hover:bg-fg-inverse/20 hover:text-fg-inverse transition-colors"
            >
              <svg
                class="w-4 h-4"
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                aria-hidden="true"
              >
                <rect x="2" y="2" width="20" height="20" rx="5" ry="5" />
                <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" />
                <line x1="17.5" y1="6.5" x2="17.51" y2="6.5" />
              </svg>
            </a>
          )}
          {business.social?.youtube && (
            <a
              href={business.social.youtube}
              target="_blank"
              rel="noopener"
              aria-label="YouTube"
              class="w-9 h-9 flex items-center justify-center rounded-lg bg-fg-inverse/10 text-fg-inverse/60 hover:bg-fg-inverse/20 hover:text-fg-inverse transition-colors"
            >
              <svg
                class="w-4 h-4"
                xmlns="http://www.w3.org/2000/svg"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
                aria-hidden="true"
              >
                <path d="M22.54 6.42a2.78 2.78 0 0 0-1.95-1.96C18.88 4 12 4 12 4s-6.88 0-8.59.46a2.78 2.78 0 0 0-1.95 1.96A29 29 0 0 0 1 12a29 29 0 0 0 .46 5.58A2.78 2.78 0 0 0 3.41 19.6C5.12 20 12 20 12 20s6.88 0 8.59-.46a2.78 2.78 0 0 0 1.95-1.95A29 29 0 0 0 23 12a29 29 0 0 0-.46-5.58z" />
                <polygon points="9.75 15.02 15.5 12 9.75 8.98 9.75 15.02" />
              </svg>
            </a>
          )}
        </div>
      )
    }
  </div>

  {
    footerExtra?.widgets?.length ? (
      <div class="border-t border-fg-inverse/10">
        <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-8">
          <WidgetArea name="footer-extra" class="widget-area footer-extra" />
        </div>
      </div>
    ) : null
  }

  <!-- Bottom bar -->
  <div class="border-t border-fg-inverse/10">
    <div
      class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-6 flex flex-col sm:flex-row items-center justify-between gap-4"
    >
      <p class="text-xs text-fg-inverse/40">
        &copy; {year}
        {business.businessName}. All rights reserved.
      </p>
      {
        legal.length > 0 && (
          <nav class="flex flex-wrap items-center gap-x-4 gap-y-1">
            {legal
              .filter((l) => l.href)
              .map((l) => (
                <a
                  href={l.href}
                  class="text-xs text-fg-inverse/40 hover:text-fg-inverse transition-colors"
                >
                  {l.label}
                </a>
              ))}
          </nav>
        )
      }
      <p class="text-xs text-fg-inverse/40">
        Built by{" "}
        <a
          href="https://extensibl.com"
          target="_blank"
          rel="noopener"
          class="text-fg-inverse/60 hover:text-fg-inverse transition-colors"
        >
          Velocity Web Studio
        </a>
      </p>
    </div>
  </div>
</footer>
